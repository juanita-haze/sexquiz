import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
});

export const metadata: Metadata = {
  title: 'Couple Quiz - Discover Your Compatibility',
  description:
    'Take our intimate quiz with your partner and discover what you both want. Only mutual matches are revealed!',
  keywords: ['couples quiz', 'compatibility test', 'relationship quiz', 'intimacy quiz'],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>{children}</body>
    </html>
  );
}
